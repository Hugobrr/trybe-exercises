let n=233;
let raiz=n**(1/2);
let listaPrimos=[1,2];
let nãoprimos=[];
let mul
for(let numero=0;numero<raiz;numero++){
    for(let multiplicador=0; multiplicador<raiz;multiplicador++){
        nãoprimos=multiplicador*listaPrimos[numero]
    }

}